/get_Options_data:

will give the inputFields with there id and values.

/get_students_data

it take inputfileds with options and it will give the students details for attendence.


/set_students_attendence ( not imlemented. reason: Clg are holidays, so there will be no student data. )

it will take the students data with attendence ( check box ). The attendence will automaticall will updated.